import { render } from "@testing-library/react";
import Register from "./component/Register/Register";

test("renders username input", () => {
  render(<Register />);
  const usernameInput = srceengetByID("userName");
});

test("renders");
